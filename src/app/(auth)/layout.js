import React from 'react';

const Authlayout = ({ children }) => {
    return (
        <div>
            <h1>new layout  is working</h1>
            <main>{children}</main>

        </div>
    );
};

export default Authlayout;